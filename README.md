# Multi-Tenant-LMS-API
A GraphQL-only Rails API for a Multi-Tenant Learning Management System (LMS). Features row-level multi-tenancy based on subdomains, JWT-based stateless authentication with Devise, and strict role-based access control (RBAC) using CanCanCan. Supports isolated school environments for Users, Instructors, and Admins.
